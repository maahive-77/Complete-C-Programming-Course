#include <stdio.h>

int main (){
    char s[10];
    gets(s); //  the Entered string is stored  in s !

    // printf("%s\n",s);
    puts(s); // puts and prints  diff hai..  puts last me new line de deta automatically but prints nhi deta  

    return 0;
}