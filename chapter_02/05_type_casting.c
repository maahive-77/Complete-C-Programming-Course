#include <stdio.h>

int main (){
    int  n = 45;
    float m = 32.32;
     
    n = (int) m; //float convert to int..
    printf("the float value is convert into int is %d",n);
    return 0;
}