// are of circle and volume of cylinder....
#include <stdio.h>

int main()
{

    int radius = 5;

    int _radius = 5;

    int height = 10;

    printf("the volume of cylinder with radius %d and height %d is %f\n", _radius, height, 3.14 * _radius * height);

    printf("the area of circle with radius %d is %f", radius, 3.14 * radius * radius);

    return 0;
}